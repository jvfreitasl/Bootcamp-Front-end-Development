const idade = 15;
const podeDirigir = idade >= 18 ? "Sim" : "Não";

console.log(`Posso dirigir? ${podeDirigir}`);

const numero = 12;
const resultado = numero % 2 === 0 ? "Par" : "Impar";

console.log(`O número é ${resultado}`);
console.log(`O número é ${numero % 2 === 0 ? "Par" : "Impar"}`);
